# The credential deliberately does not live here: pnpm ignores environment variables in
# registry credentials read from a committed project .npmrc. See the README's Installing
# section for where to put it instead.
@quantumblueconsulting:registry=https://npm.pkg.github.com
